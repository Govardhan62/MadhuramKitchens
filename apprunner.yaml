version: 1.0
runtime: python3.12  # Make sure this matches your Python version
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.9  # Match this with the runtime version you are using
  command: sh ./startup.sh
  network:
    port: 80  # Update to port 80
