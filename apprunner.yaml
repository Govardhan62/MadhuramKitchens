version: 1.0
runtime: python3
build:
  commands:
    build:        
      - pip install pipenv
      - pipenv install
      - pip install -r requirements.txt
run:
  runtime-version: 3.8.16
  pre-run:  
    - pip3 install gunicorn
    - pip3 install -r requirements.txt
    - which gunicorn
  command: gunicorn --workers 4 config.wsgi:application --bind 0.0.0.0:8080
  network:
    port: 8080
