version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.8.16
  command: sh ./startup.sh
  network: 
    ports:
  - port: 8191
    name: db.sqlite3
    appProtocol: http
  - port: 80
    name: http-web
