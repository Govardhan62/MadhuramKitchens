version: 1.0
runtime: python311
build:
  commands:
    build:
      - python3.11 -m ensurepip --upgrade # Upgrade pip (optional)
      - pip3 install Django # Install Django and dependencies
      - pip3 install -r requirements.txt
    post-build:
      - python manage.py makemigrations
      - python manage.py migrate
      - python manage.py collectstatic
  env:
    - name: DEBUG
      value: "on"
    - name: SECRET_KEY
      value: 'django-insecure-r(vctop4h_)2ciq#!38tr(bsr^qoyr%t$7*buweve1ikl=&1%0'
    - name: ALLOWED_HOSTS
      value: "*"
run:
  runtime-version: 3.11
  command: pip3 install gunicorn config.wsgi # Install gunicorn at runtime
  pre-run: [] # Remove pre-run section
  command: gunicorn config.wsgi --log-file -
  network: 
    port: 8080
    env: 8080  
  env:
    - name: DEBUG
      value: "on"
    - name: SECRET_KEY
      value: 'django-insecure-r(vctop4h_)2ciq#!38tr(bsr^qoyr%t$7*buweve1ikl=&1%0'
    - name: ALLOWED_HOSTS
      value: "*"
    - name: MONGO_DB_NAME
      value: "madhuram" 
    - name: MONGO_DB_HOST
      value: "mongodb+srv://madhuramkitchens.e5AaQ9ZFofmCmzyM@cluster0.nsum0.mongodb.net/your_db_name?retryWrites=true&w=majority"
    - name: MONGO_DB_USERNAME
      value: "madhuramkitchens"
    - name: MONGO_DB_PASSWORD
      value: "e5AaQ9ZFofmCmzyM"

