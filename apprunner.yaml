  version: 1.0
  runtime: python3.12

  build:
    commands:
      - pip install --upgrade pip
      - pip install -r requirements.txt
      - chmod +x startup.sh  # Make the startup script executable

  run:
    command: ./startup.sh